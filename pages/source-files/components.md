---
title: Components
---

# Components
A `component` characterizes an entity as possessing a particular quality or capability, and holds the data needed to model it.<br/>
Components are pure data, they contain no logic of their own.

## Defining Components
You can define a component using the `component` keyword followed by the component name and its data type.
```daisy
component Health = u8
```